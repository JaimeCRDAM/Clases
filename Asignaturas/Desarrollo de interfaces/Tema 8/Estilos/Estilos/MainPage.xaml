<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Estilos.MainPage">
    <ContentPage.Resources>
        <!--
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="#000200"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="FontSize" Value="Large"/>
            <Setter Property="TextColor" Value="{StaticResource txtColor}"/>
        </Style>
        <Style TargetType="Button" x:Key="buttonStyle">
            <Setter Property="BackgroundColor" Value="{StaticResource bgColor}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="FontSize" Value="Large"/>
            <Setter Property="TextColor" Value="{StaticResource txtColor}"/>
        </Style>
        -->

    </ContentPage.Resources>
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">
            <Button Text="LOGIN"/>
            <Button Text="Visit Website" Style="{StaticResource specialButton}"/>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
